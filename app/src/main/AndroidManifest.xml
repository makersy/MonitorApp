<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sust.monitorapp">
    <!-- 开放网络权限，否则使用网络时会报错 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:name=".common.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.AddDeviceActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".activity.DeleteDeviceActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".activity.ModifyDeviceInfoActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.DeviceMoreInfoActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.SelectDeviceActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.UserMoreInfoActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.ModifyUserInfoActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.DeleteUserActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.ModifyOwnInfoActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.UserOwnInfoActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.SelectUserActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.AdminMainActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.UserMainActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.SignInActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.ChangePasswordActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="毕业设计" />
    </application>

</manifest>